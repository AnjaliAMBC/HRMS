<div class="admin-empadd-view">
    <div class="container">
        <!-- Headline: Import Employee -->
        <h3 class="import-user-head">Import Employee</h3>

        <!-- Description with bullet points -->
        <div class="description">
            <p>Here's how to import employees:</p>
            <ul>
                <li>Prepare your employee data in a CSV format.</li>
                <li>Make sure the CSV file follows the correct format.</li>
                <li>Use the template provided for best results.</li>
            </ul>
        </div>

        <div>
            <button class="btn btn-primary download-btn" onclick="downloadTemplate()">Download Template</button>
        </div>
        <p style=" margin-bottom: 72px;">Sample excel template</p>

        <div class="file-upload" id="file-upload-area">
            <label for="file-upload-input" id="file-upload-label">
                <i class="fas fa-cloud-upload-alt fa-3x mb-3"></i><br>
                Drag and drop files or Browse<br>
                Supported formats: .XLSX
            </label>
            <input type="file" id="file-upload-input" accept=".xlsx" onchange="handleFileUpload(this)">
        </div>

        <div class="uploaded-file" id="uploaded-file-info" style="display: none;">
            <p style="color: green;" id="uploaded-file-text"></p>
        </div>

        <div class="submit-button">
            <button class="btn btn-importuser-submit" style="background-color:#2693F8">Submit</button>
        </div>

        <div class="modal fade" id="importSuccessModal" tabindex="-1" role="dialog" aria-labelledby="importSuccessModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="importSuccessModalLabel">Import Users</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <p class="success-message">Users imported successfully!</p>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-primary refresh-emptablist">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
